nel body vengono visualizzare le tabs a seconda di quello che si vuole vedere
per cui per ogni tab viene detto che cosa ogni ruolo deve vedere
in ogni possibile tab c'è una gestione per tutti i ruoli